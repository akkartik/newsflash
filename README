$ mkdir db/columns

Add your RSS feeds into db/columns/feeds

$ rake --trace feeds:update
You may need to install http://rfeedparser.rubyforge.org

Start your server:
$ script/server

Go to localhost:3000
